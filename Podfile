# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'EventsSi' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for EventsSi
  pod 'Kingfisher', '~> 5.0'
  
  target 'EventsSiTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
